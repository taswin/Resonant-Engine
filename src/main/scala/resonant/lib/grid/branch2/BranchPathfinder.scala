package resonant.lib.grid.branch2

/**
 * A pathfinder that generates branches in a grid that are collapsable
 * Uses multithreading
 * @author Calclavia
 */
class BranchPathfinder(grid: GridBranch)
{
  def start()
  {
    val nodes = grid.getNodes
  }
}
